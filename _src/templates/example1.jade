include mixins/form-control
html(ng-app="PolyForm")
  head
    include partials/header
  body
    .container
      form(name="form", ng-cloak="true")
        form-tabs
          form-tab
            +form-control({name:'fname', popover: 'Παρακαλώ συμπληρώστε το πεδίο'})
            +form-control({name:'lname', isUnique: 'isUnique.json'})
          form-tab
            +form-control({name:'fname', popover: 'Παρακαλώ συμπληρώστε το πεδίο'})
            +form-control({name:'lname', isUnique: 'isUnique.json'})

    include partials/footer