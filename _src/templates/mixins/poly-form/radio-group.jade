include form-input

mixin radio-group(options)
  - options       = options               || {}

  - className     = options.class         || 'col-md-12'
  - inputClass    = options.inputClass    || 'form-control'
  - radioClass    = options.radioClass    || 'radio-inline'
  - name          = options.name          || 'control'
  - id            = options.id            || (options.name || 'controlRadio')
  - model         = options.model         || (options.id || '$parent.$parent.'+id)
  - label         = options.label         || 'Ετικέτα'
  - placeholder   = options.placeholder   || label
  - autocomplete  = options.autocomplete  || name
  - type          = options.type          || 'radio'
  - popover       = options.popover       || null

  - collection    = options.collection    || null

  - options.inputClass = 'form-input'

  .row
    div(class=className)
      if (collection)
        each val, index in collection
          - options.ngChecked = index==0?'true':null
          div(class=radioClass)
            label
              - options.value = (typeof val == 'string' || typeof val == 'number' ? val: val.value)
              +form-input(options)
              = ' ' + (typeof val == 'string' || typeof val == 'number' ? val: val.label)
      else
        label
          .input-group
            +form-input(options)
            = ' ' + label
